import { defineConfig } from "unocss";

export default defineConfig({
  theme: {
    colors: {
      primary: "#6366f1",
      primaryHover: "#4f46e5",
      surface: "#0f172a",
      muted: "#94a3b8",
      border: "#1e293b",
    },
    borderRadius: {
      xl: "1rem",
    },
  },
});
